You are the SpecPT experimenter. Generate the next experiment config.

Inputs:
- Analyst output: {"metrics":{"best_nmad":0.01506,"final_nmad":0.01727,"final_outliers":24.07,"val_rmse":0.4448,"val_loss":0.3719,"epochs":400},"comparison":"worse","diagnosis":"Severe overfitting: train_loss (0.038) 10x lower than val_loss (0.372). NMAD peaked at epoch 373 (0.01506) then degraded in final 27 epochs. Catastrophic outliers dropped from 86% to 24% but remain high. LR barely decayed (1e-4 to 8e-5), scheduler did not trigger meaningfully.","recommendation":"tune_regularization","recommendation_reason":"Model overfits severely (10x train/val loss gap); stronger regularization or higher dropout/weight_decay could improve generalization and push NMAD below 0.01382."}
- Project root: F:\personal_projects\specpt-hst-sim

Workflow:
1. Read ./EXPERIMENTS.md (root) and .hermes/SOUL.md for full history
2. Read configs/defaults.yaml as the base config
3. Choose ONE change from the VALID list below (NEVER repeat a change already in EXPERIMENTS.md)

CRITICAL CONSTRAINT — AUTOENCODER IS FROZEN
The SpecPT autoencoder (conv layers + transformers) is pretrained and frozen.
NEVER change these autoencoder params — they would break checkpoint loading:
- model.input_size = 7781 (FROZEN)
- model.d_model = 512 (FROZEN)
- model.nhead = 8 (FROZEN)
- model.num_encoder_layers = 3 (FROZEN)
- model.num_decoder_layers = 3 (FROZEN)
- model.dim_feedforward = 2048 (FROZEN)
- model.dropout = 0.1 (FROZEN, this is the autoencoder dropout)

Only the redshift estimator head can be modified. Valid changes (pick ONE):
- Redshift head: num_mlp_blocks (3,5,7,10), mlp_dim (128,256,512,768), dropout_rate (0.05,0.1,0.2,0.3)
- Training: lr (5e-5,1e-4,2e-4,5e-4), batch_size (32,64,128,256), epochs (200,400,600,800)
- Optimization: patience (20,50,100), weight_decay (1e-5,5e-5,1e-4,5e-4)

Key history:
- exp_021 (just finished amber-gorge-34): patience 50->100, batch=256, mlp_dim=1024, 12 blocks, lr=1e-4, wd=5e-5, warmup=50. Best NMAD 0.01506 (better than exp_020's 0.01909 but worse than all-time best 0.01382). Train/val loss gap ~10x. Patience increase helped moderately.
- Past regularization attempts all failed: dropout (0.1, 0.2) and weight_decay (1e-5, 5e-4) all degraded NMAD.
- exp_019 (batch=256, warmup=500): NMAD 0.0167, LR never reached full value due to long warmup.
- exp_020 (warmup 500->50): NMAD 0.01909, LR ascended but still degraded.
- exp_013 all-time best NMAD 0.01382 used: mlp_dim=1024, 12 blocks, batch=128, lr=1e-4, wd=5e-5, warmup=500.

The analyst recommends "tune_regularization" but this has been tried and failed.
Better options based on history:
- The patience increase (50->100) helped moderately. The model trains longer now.
- The model is overfitting severely but regularizers have all failed.
- Next direction should consider: reverting batch_size from 256 back to 128 (exp_013 used batch=128 and achieved best NMAD), or trying a different learning rate.

4. Write the config file to configs/exp_022.yaml using defaults.yaml as base
   - The previous config (exp_021) had: patience=100, batch_size=256, mlp_dim=1024, num_mlp_blocks=12, lr=1e-4, wd=5e-5, warmup=50, epochs=400
   - You need to read configs/exp_021.yaml to know what exp_021 changed from defaults.yaml

5. CRITICAL: ALSO add a row to ./EXPERIMENTS.md Running table BEFORE returning:
   | exp_022 | configs/exp_022.yaml | — | — | — | — | — | — | — | — | <description> |
   This is MANDATORY — do not skip.

Return ONLY a JSON object (no prose, no markdown) on a single line:
{"exp_name":"exp_022","config_path":"configs/exp_022.yaml","change_description":"<what changed and why>","parameter_changed":"<which param>","old_value":"<previous>","new_value":"<new>"}
